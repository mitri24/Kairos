node_modules/
.DS_Store
_site/

# Deployment-Secrets (u. a. VAPID_PRIVATE_KEY, DOMAIN) — Template bleibt versioniert.
# .env.* matcht auch .env.example, daher die Negation danach (Reihenfolge zählt).
.env
.env.*
!.env.example

# Lokale SQLite-Datenbank (Laufzeit)
server/data/
*.db
*.db-wal
*.db-shm

# Swift-Build-Artefakte (Pace) — rebuildbar via `swift run`
Pace/.build/
Pace/Pace.app/
